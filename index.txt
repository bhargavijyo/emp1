//Javascript 
Js is a programming language used to create interactive 
and dynamic user responsive web pages.

why we use 
1.it can manipulate data and handling data 
2.make API requests 
3.perform calculations 

//variables  -- used to store data
1.var  -- It can be redeclare and updated
2.let -- It can't be redeclare but we can update
3.const -- It can't redeclare and update

// Console methods-- It displays the output data  or
interact with user on different areas of browser's developer 
1.console.log -- It prints data directly 
2.console.error -- It displays the error msseges
3. console.warn -- It displays the warning 
4.console.clear -- It clears the all content in the web page 

//Printing statements 
1.console.log()--
2.alert()-- It displays a pop-up dialog box with a specified msg
3.confirm()--This is also same as alert dialog box but it shows msgs like (y/n)
4.prompt()-- Prompt also a display dialog box but in this user can give input what user needs
5.document.writeln()--  writes a content directly into the web page

//Data types 
1.primitive -- numbers, strings, boolean, undefined and null
2. Non-primitive -- Arrays , object, function, date

//Type conversion -- It is the process of changing a value from one data type to another 
1. Implicit type -- In this Automatically convert one data type to another 
2.Explicit type -- In this The programmer automatically converts one type to another

//Flow control statements -- It determines the order in which codes executed
1.conditional statements -- It executes based on condition 
    1.if statement --  Executes the block of code if condition is true 
        syntax: 
            if(condition){

            }
    2.if...else -- if condition is true otherwise else block 
        syntax:
            if(){
                stmt
            }else{
                stmt
            }
    3.else if -- In this we are check multiple conditions 
        syntax: 
            if(){
                
            }
            else if(){

            }else if(){

            }else{

            }
    4.switch -- selects one of many code blocks to execute 
        syntax:
            switch(condition){
                case 1:
                break;
                case 2:
                break;
                ....
                default;
            }
    5.Nested if -- If statement placed completely inside another if....
    in this way it will evaluate all conditions ,
    if any condition is false it will stop execution there.
    if(){
        if(){

        }else{

        }
    }else{
        
    }


//Nodes js -- javascript run time environment that allows us to run javascript
                code outside the web browser 
                we can directly run on server side 
                frontend - javascript/reactjs 
                backend - javascript/node js 

                //uses 
                web servers 
                rest apis
                backend applications 
                real-time applications 

//differences --
js:  programming language 
     used to write js code 
     it can run in any browser like edge,chrome,firefox 
     provides browser apis like windows,document 
Node.js:  Run time environment 
          used to execute javascript outside the browser 
          Run on operating systems/servers 
          server side apis like http,ftp,fs,path 
//v8 
v8 is a browser to acts as a javascript engine developed by google, 
Node.js uses this v8 js to execute the js code .

2.Looping statements -- used to execute a block of code until a condition met.
    1.for loop -- known number of conditions 
        syntax:
            for(initialization;condition;inc/dec){
                stmt;
            }
    2.while loop -- Executes the code as long as the condition is true.
        syntax:
            while(condition){
                stmt;
            }
    3.do...while -- Executes the code atleast once, then checks the condition 
        syntax:
            do{
                stmt;
            }
            while(condition);
    4. for...of -- used to iterate over the value of iterable objects like arrays and strings.

    5.for...in --Used to iterate over the properties of an object.

//Functions -- it is a reusable block of code that performs specific task.
                These functions can help make code organized,reusable, and easier to maintain 
                
            //callback function -- It used to pass an argument to another 
                                    function
            
            //IIFE(Immediately invoked function expression)--
            Executes Immediately after it is defined
            ()

//Operators --operators used to perform an operation on variables 
    1.Arithmetic  -- +,-,*,/,%,**,++,--
    2.Assignment -- =,+=,-=,*=,/=,%=,**=
    3.Relational & comparison -- ==,===,!=,!==,>,<,>=,<=
            ==(compares values after type conversion )
            ===(It compares both value and type)
    4.Logical operators -- &&, ||, !
                && (when all conditions are true then answer/output must be true)
                ||(anyone one condition is true output is true)
                !(reverse the result)
//Data structures --
    1.spread operator(...)-- The spread operator used to spread / unpack the elements
        of an array or properties of an object into another array,object or function call.

    2.Rest operator(...)-- used to collect multiple values into a single array.

    3.Destructuring -- Extracts values from arrays 
                        properties from objects and store them in variables 
                1.Array --
                2.Object -- 

//Es6 Array methods --
            ES6 -- ECMScript6 it introduced new features that help 
                    us to write modern,clean,and maintainable javascript code.

            1.manipulation -- add or removing elements 
                                push -- Adds one or more elements to the end of an array 
                                        syntax : array.push(element);
                                pop -- remove an element 
                                        syntax: array.pop();
                                unshift -- Adds one or more elements to the begining of an array.
                                shift -- Removes the first element from an array.
                                splice  -- It is used to add, remove or replace elements at any position .
                                        syntax: array.splice(start, deleteCount,item1,item2...);
            2.Merge -- 
                    1.concat() -- combines two or more arrays 

                    2.slice() -- Returns a portion(sub part) of an array without changing original array.
                                syntax: array.slice(start, end);
                    3.flat() -- converts a nested array into  a flatter array.

                    4.fill() -- replace array elements with a specified value .

                    5.includes() -- Checks whether an array contais a specific value.
                                    then it return true or false.
                    
                    6.sort() -- sorts the elements of an array.

                    7.indexof() -- Returns the first index where an element if found

                    8.lastIndexOf() -- Return the last element where an element occurs

                    9.reverse() -- Reverses the order of elements.
            3.To string -- 
                    1.join() -- converts array elements into a string using a seperator.

                    2.toString() -- converts array into a string.
            4.Higher-order Array methods -- A higher-order function is a function that:
                                            accepts another function as an argument or returns a function 
                                            simply receive a callback functions.

                    1.some() -- checks whether at least one element satisfies a condition.

                    2.every() -- checks whether all elements satisfy a condition.

                    3.forEach() -- Executes a function once for every array element.

                    4map() -- Creates a new array by modifying each element .
                    transforms every element and return new array.

                    5. filter() -- Creats a new array containing only 
                                    elements that a satisfy a condition.
                                    selects matching elements.
                    6.find() -- Returns the first element that satisfies a condition .
                                    it returns only one element 
                    
                    7.reduce() -- Reduces all array elements to a single value.
                                    similar to sum 
//String methods -- A string is a sequence of characters used to store and manipulate text or data.
            1.length -- Returns the number of characters in a string.

            2.charAt(index) -- Returns the character at the specified index.

            3.at() -- Returns the character at the specified index.
                        It also supports negative indexes.

            4.indexOf() -- Returns the index of the first occurence of a specified value.

            5.lastIndexOf() -- Returns the index of the last occurence of a specified value.

            6.includes() -- Checks whether a string contains specified value or not.

            7.startWith() -- Checks whether a string starts with a specified value .

            8. endsWith() -- Checks whether a string ends with a specified value .

            9.slice() -- portion 

            10.substring() -- returns characters between two indexes.

            11.replace() -- It replaces the first matching value.

            12. replaceAll() -- replaces all matching values.

            13.toUpperCase() -- convert entire string to uppercase.

            14.toLowerCase() -- convert entire string to lowercase.

            15.trim() -- Removes spaces from both ends of a string.

            16.trimStart() -- Remove spaces at the begining of a string.

            17.trimEnd() -- Remove spaces at the end of a string.

            18. split() -- splits string into a array.

            19.concat()-- 

            20.repeat() -- Repeats a string a specified number of times.

            21.padStart() -- Pads the begining of a string.

            22.padEnd() -- Pads the ending of a string.

            23. search() =~ indexOf()

            24. match() -- returns the matched text based on a pattern.


//Date Methods -- Used to create a dates and times.
                    get the current date and time 
                    creates a specific date
                    retrieve individual parts of a date(year,month,day,hours, etc)
                    modify dates and times
                    format dates into readable strings.

                    syntax: 
                            let date = new Date();
            //Date getter methods 
                    1.getFullYear() -- returns the year.
                    2.getMonth() -- Returns month(0-11)
                    3.getDate() -- returns the day of month(1-31)
                    4.getDay() -- Returns the day of week(0-6)
                    5.getHours()-- returns the hour(0-23)
                    6.getMinutes() -- returns the mints(0-59)
                    7.getSeconds() -- (0-59)
                    8.getMilliseconds()--(0-999)
                    9.getTime() -- returns the number of millisecs since 1,1970(unix epoch)

            //Date setter methods : 
                    1.setFullYear()-- sets the year
                    2.setMonth() 
                    3.setDate()
                    4.setHours()
                    5.setMinutes()
                    6.setSeconds()
                    7.setMilliseconds()
            //Date Formatting -- 
                    1.toDateString() -- Returns only the date in a readable format.
                    2.toTimeString() -- returns only time.
                    3.toLocalDateString() -- Returns the date based on user's locale.
                    4.toLocaleTimeString() --Returns the time based on user's locale.
                    5.toLocaleString() -- Returns both date and time in local format.
                    6.toISOString() -- returns the date in ISO 8601 format.
            //utc methods -- These methods work with coordinated universal time(utc) instead of local time.
                    1.getUTCFullYear()
                    2.getUTCMonth()
                    3.getUTCDate()
                    4.getUTCDay()
                    5.getUTCHours()
                    6.getUTCMinutes()
                    7.getUTCSeconds()

//Asynchronous methods : Asynchronous js allows the program to perform long-running tasks(
                            such as fetching data,reading files, or waiting for a timer)
                            it will executes time consuming operations  in the background and 
                            continues executing other code instead of waiting.
                1.setTimeout() -- executes a function once after a specified delay.
                                syntax: 
                                        setTimeout(function,millisecs);
                
                2.setInterval() -- repeatedly executes a function after a fixed time interval.
                                    syntax: 
                                            setInterval(function,millisecs);
                3.clearTimeout() -- Stops a timeout before it executes.

                4.clearInterval() -- stops an interval 

                5.promises -- Represents the failure of an asynchronous operations.
                                1.pending
                                2.Fullfilled(Resolved)
                                3.Rejected 

                                syntax: 
                                        let promise = new Promise((resolve,reject)=>{

                                        });
        //async -- it makes a function returns a promise .
        //await -- it pauses execution inside that function until the promise is resolved.

//synchronous (blocking) -- Executest one statement at a time 
                            the next line waits until the current line finishes.

//DOM -- It is an application programming interface(api) for manipulating HTML Documents.
         Document object model is a tree like representation of an html document.
        Js uses the dom to access , modify,add,or remove html elements dynamically 

        1.selecting :  means finding or selecting html elements so that js can work with them.
                    1.getElementById() -- selects an element using id.

                    2.getElementsByClassName() -- selects all elements with the given class 

                    3.getElementsByTagName() -- selects elements by tag name.

                    4.querySelector() -- selects the all matching element.
                    5.querySelectorAll() -- selects the all matching element.

        2.Traversing : Traversing means moving from  one DOM element to another(parent, child,sibling)
                    1.parentElement
                    2.children
                    3.firstElementChild 
                    4.lastElementChild
                    5.nextElementSibling
                    6.previousElementSibling

        3.Manipulation : changing HTML elements ,styles, or attributes using javascript.
                            1.change text 
                            2.chang Html 
                            3.Change css styles
                            4.change Attribute 
                            5.create new element
                            6.remove element 
                            7.add a class


//Node.js modules 
                A module is a reusable block code that is organized into a separate file.
                It is used to divide a large application into smaller files.
                ex: E-commerce website 
                                user.js 
                                product.js
                                cart.js
                                payment.js
                                app.js
                types :
                    1.core/built-in modules: 
                                fs - fileSystem 
                                http - server 
                                path - file and directory paths 
                                os - operating system information 
                                events - handling events 
                                url - url handling
                    2.user module -- the user created data 
                            app.js 
                            calc.js
                            function add(a,b){
                                return a+b;
                            }
                    3.Third party modules -- npm(node package manager)

    //importing module : require()
                        const fs = require("fs); 
                                    require(load modules)
                                    file system module 
    //export module : 
                   module.export =;
                            



                






